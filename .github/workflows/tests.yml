name: Tests

on:
  push:
  pull_request:

jobs:
  tests:
    name: "PHP ${{ matrix.php }} — Laravel ${{ matrix.laravel }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ["8.2", "8.3", "8.4"]
        laravel: [11, 12, 13]
        include:
          - laravel: 11
            testbench: "^9.0"
          - laravel: 12
            testbench: "^10.0"
          - laravel: 13
            testbench: "^11.0"
        exclude:
          # Laravel 13 exige PHP ^8.3
          - php: "8.2"
            laravel: 13

    steps:
      - uses: actions/checkout@v5

      - name: Configurer PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Cache Composer
        uses: actions/cache@v5
        with:
          path: ~/.composer/cache
          key: composer-php${{ matrix.php }}-laravel${{ matrix.laravel }}-${{ hashFiles('composer.json') }}
          restore-keys: |
            composer-php${{ matrix.php }}-laravel${{ matrix.laravel }}-
            composer-php${{ matrix.php }}-

      - name: Installer les dépendances (Laravel ${{ matrix.laravel }})
        run: |
          # Composer 2.9+ bloque l'install si des advisories de sécurité
          # existent sur les dépendances (feature policy.advisories.block).
          # Les CVEs visés concernent laravel/framework et non ce package.
          composer config --no-interaction policy.advisories.block false
          composer require \
            "illuminate/support:^${{ matrix.laravel }}" \
            "illuminate/http:^${{ matrix.laravel }}" \
            "orchestra/testbench:${{ matrix.testbench }}" \
            --no-interaction --no-update
          composer update --prefer-dist --no-interaction

      - name: Lancer les tests
        run: vendor/bin/phpunit
